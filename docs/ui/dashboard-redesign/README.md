# 界面重设计（0.8.0 → 0.8.5，六个页面）

按「**极简 + 用户友好**」重排界面。0.8.0 只改仪表盘；0.8.1 继续改了日志页与设置页
（见文件末尾）。节点、订阅、规则三页仍是原样。

| 文件 | 说明 |
|---|---|
| `01-before.png` | 改动前的仪表盘（1080×720，与 Tauri 窗口一致） |
| `02-after-uncommitted.png` | 改动后 · 两阶段启动的中间态（隧道已建、路由未接管） |
| `03-connected.png` | 改动后 · 已连接 |
| `04-disconnected.png` | 改动后 · 未连接 |
| `05-notices.png` | 改动后 · 多条提示时只显示最急的一条 |
| `06-min-window.png` | 改动后 · 最小窗口 900×580（检查响应式不溢出） |

截图由 `Chrome --headless=new --screenshot` 渲染，数据来自
`apps/ui/src/preview.ts` 的合成快照（浏览器预览桥接，见下）。

## 改了什么，以及为什么

### 1. 「现在通不通」从三处合并为一处

旧版把连接状态拆在三个地方：一张写着 pid 的卡片、顶栏的绿点与「断开」按钮、
以及一条黄色横幅（「路由尚未接管」）。用户得自己把三处拼成结论 ——
而 `docs/05` 第 2 条明确要求「连接成功必须精确表达」。

现在是一个状态区：状态词（带颜色）+ 节点 + 延迟徽章，下面一行把
「模式 · 路由 · 已运行时长」串起来，右侧是唯一的主操作按钮。
**中间态（隧道已建、路由未接管）成为状态词本身**，不再是第四条横幅。

### 2. 横幅从最多 5 条收敛为 1 条

`Notices` 原本有 5 个平铺的 `{cond && <Banner/>}` 分支（路由未接管、
残留会话、找不到核心、核心太老、后端提示），加上底部的 `last_error`，
顺序是**代码顺序而非紧急程度** —— 于是「流量还没走代理」可能排在
「helper 没装」下面。现在统一收集后按严重度排序，只显示最急的一条，
其余折成一行「还有 N 条提示」。

### 3. 流量与诊断降级

* 流量原本是两张占满的卡片、与「当前节点」同权重。现在是一行紧凑文本
  （等宽字体 + 定宽，数字跳动不推挤布局）。
* 进程 pid、配置路径、上次配置变更收进「环境自检与诊断」折叠区 ——
  出问题时才需要看。
* 卡片网格的错位（`minmax(240px)` 在 1080 窗口下排 3 列，第 4 张被迫换行留白）
  随卡片一起消失。

### 4. 直连模式下说明原因

直连模式里连接按钮是禁用的（这是对的：那里该切模式而不是启动一个没用的进程），
但旧版只禁用、不解释。现在禁用时给出一句原因。

## 顺带加的：浏览器预览桥接

`apps/ui/src/preview.ts` + `main.tsx` 里的一段 dev 分支。

**为什么**：界面完全依赖 Tauri 的 `invoke`，在普通浏览器里只会看到
`window.__TAURI_INTERNALS__ is undefined`。于是每改一次样式都要重编整个 Rust
外壳（几分钟），而样式迭代本该是秒级。挂上桥接后布局、样式、交互都能真实渲染。

```bash
cd apps/ui && npm run dev
# 浏览器打开 http://localhost:5173/?preview=1
# 切换场景：&state=connected|uncommitted|disconnected|no-core|stale|notice
```

**生产不受影响**：只在 `import.meta.env.DEV` 为真且 URL 带 `?preview=1` 时
动态加载。`npm run build` 之后已核实产物里不含
`installPreviewBridge` / `scenarioSnapshot` / 任何合成节点数据。

---

## 0.8.1 追加：日志页与设置页

| 文件 | 说明 |
|---|---|
| `07-logs.png` | 日志页：等级计数、行首色条、来源列 |
| `08-settings.png` | 设置页：分组 + 锚点导航 |

日志页的三处实际改进：**等级筛选带计数**（不用点进去就知道有没有错误）、
**行首 2px 色条**（只靠文字颜色在一屏等宽文本里很难扫）、
**来源列**（排查时常要先分清是 App 说的还是核心说的）。

设置页把 9 张带边框的卡片改成四组 + 跟随滚动的锚点导航；
**表单逻辑一行未改**，只加了分组容器与锚点。

---

## 0.8.5 追加：节点、订阅、规则三页

至此六个页面全部统一到同一套语言（`page` / `page__sec` / `page__title`），
仪表盘用 `dash__*`。

| 文件 | 说明 |
|---|---|
| `09-nodes.png` | 节点页：徽章带语义标签、当前节点有强调条 |
| `10-subscriptions.png` | 订阅页：用量条与失败态 |
| `11-routing.png` | 规则页：预设改成单选控件 |

### 节点页最实际的一处改动

「距离」与「能不能用」原本只写在 `title` 悬停提示里，界面上一眼看到的是
一个绿色 `53 ms` 紧挨着一个红色「不可用」—— 代码注释自己就记着这个矛盾。
现在徽章上直接带标签（`距离 53 ms` / `可用`），**不用悬停也读得对**。

列表页**保留高密度**（一屏 4-8 个节点），只减装饰性重量：行内的
「二维码 / 删除」收成弱按钮，选中态用左侧强调条 + 「当前」标签。

### 订阅页

用量条加粗到 6px（4px 时在 1080 窗口下几乎看不出余量，用 CDP 实测确认过
填充确实存在、只是太细），并在余量偏低/即将用尽时变色 + 给出文字结论。
失败态补一句「已有节点仍然可用，可以稍后重试」—— 用户才知道要不要慌。

### 规则页

四个预设原本是普通列表行 + 一个绿点，看起来像在陈列信息而不是让人选择。
改成真正的 `radio` 控件 + 选中态强调 + 「当前」标签。
「配置落盘位置」收进折叠区（排障时才需要）。
