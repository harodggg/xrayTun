# 更新记录

## 0.8.34

> **急救版**，修两个**用户正在受影响**的缺陷：
> ① `direct` 出站把**已经解析好的 IPv4 目标改写成 IPv6**，而本机没有可用 v6
> ⇒ **国内站点时不时打不开**（境外正常）；
> ② 看门狗**把还活着的隧道拆掉** ⇒ 重建失败后退回直连并作废自动重连，
> 实测空档 **2657s / 691s / 2610s**（另有一次隔夜 **48694s**）。

### 修复：国内直连被改写成 IPv6 —— 本机没有可用 v6 时，国内站点会间歇性打不开（task-97，P0）

**症状**：国外正常，**国内站点时不时打不开**。

**根因**：`direct` 出站的 `sockopt.domainStrategy` 是 `UseIP` ⇒ 核心自己解析域名并拨解析出的 IP，
把客户端**本来给的 IPv4 目标**（sniff 成域名之后）**改写成 AAAA**；而这台机器**没有可用的 IPv6**
（`ifconfig en0` 只有 `fe80::…%en0`，无 v6 默认路由）⇒ 5 次 v6 尝试后放弃。

**实测（本机 `app.jsonl` 2026-09-22 全天 123,541 行，按 session id 归并 —— `a702010` 的口径）**：

| 类别 | 连接数 | 随后失败 |
|---|---|---|
| **只有 v6 目标改写** | 42 | **40（95.2%）** |
| **只有 v4 目标改写** | 408 | **0（0%）** |

`replace destination with tcp:[240e:` 出现 **534 次**；47 条失败连接里 **40 条**是纯 v6 dial（85%）。

**改法**：`UseIPv4`（只解析 A；无 A 时按官方文档回退 `AsIs`，不是硬失败）。
* **不选** DNS 层的 `queryStrategy: UseIPv4` —— 那会连客户端要的 AAAA 一起吞掉、并波及境外解析；
* **也不用** `AsIs` —— 官方文档写明它走 Go Happy Eyeballs，TCP 仍优先 IPv6，等于没修；
* 代价（写在这里）：**双栈用户对国内站点也走 v4**；
* 境外/代理路径**一律没动**（节点出站 `sockopt` 仍是 null；DNS `queryStrategy` 保持用户设置，测试钉住）。

**回归 + 双向敏感性**：把值改回 `UseIP` ⇒ `direct_outbound_never_picks_the_v6_family` 与
`direct_outbound_binds_the_physical_interface` **当场变红**。

### 修复：看门狗不再把**还活着**的隧道拆掉（task-98，P0）

**症状**：隧道其实在转发流量，却被判「挂了」⇒ 重建 ⇒ 重建失败 ⇒ **退回直连 + 作废自动重连意图**。
`python3 scripts/net-metrics.py --until 14:21:32`（**窗口＝全部记录 → 打 tag 时刻 2026-09-22 14:21:32**；
`app.1.jsonl` + `app.jsonl` 合并、按 `(ts, message)` 去重；窗口内 **305,742** 条）：
**「已作废」7 次**，到下一次 core 启动的空档（**口径A**：作废 → 下一次 core 启动）分别是
**48694s / 2657s / 691s / 2610s / 20s / 50s / 23s**；**「隧道已自动恢复」3 行**。
两点必须连口径一起看：① **旧解析器口径会把那 3 行漏成 0**（`dd95fdb` 修掉了「一行含两个 JSON 对象 ⇒
整行丢掉」）；② **这个数随时点增长** —— 就在打 tag 前后（**14:16:41、14:19:09**）又各发生一次「已作废」，
说明该缺陷**当下仍在触发**（现场原始行见 `docs/verification/RELEASE-v0.8.34.md`）。

六处改动：

1. **轮内重试（主杠杆）**：`PROBE_ATTEMPTS_PER_ROUND = 2` —— 同一目标失败后再试一次。
   依据（实测）：「失败后下一次同目标」成功 baidu **31/33 = 94%**、cloudflare **5/9 = 56%**；
   粗算挡掉一次误判约 **99.6%**、另两次各约 **81%**。快失败与「挂住」两种病都覆盖；
2. **重定义「一轮失败」**：从「任一目标失败」改成「**本轮 ≥2 个目标失败**」
   （目标数 <2 时退化为全部失败 —— 否则门槛永远够不到、等于把自愈关掉）；
   连续计数（`ProbeStreak`）只认这种轮，其余轮清零；
3. **探针超时 6s → 10s**：覆盖实测 **p99 = 7223ms**（实测还有 6 条成功连接 >6s，会被旧的 6s 判据误杀）；
   代价「真挂住的连接 +8s/轮」写在常量注释里；
4. **重建失败不许拆掉还活着的隧道**：新增与探针**正交**的判据
   `after_failed_rebuild(stop_failed, core_process_alive(pid))` —— 停止失败**或**核心进程仍在
   ⇒ **保持原状**：不退直连、**不作废自动重连意图**、不写任何结局，只落盘一笔并**按退避重试（30/60/120/300s）**；
   只有老隧道确实没了才退直连（这条路径保持 task-64/75 的既有语义）；
5. **可诊断性**：探针轮结果（几个目标失败 / 连续第几轮 / 哪条目标死 / 是「整侧不通」还是「零散失败」）、
   重建原因与结果改用 `state.log` **落盘**（此前 `push_log` 不落盘 —— `source=app` 的行数少得可怜：
   **本机** `app.1.jsonl` + `app.jsonl` 在 **2026-09-22 14:19** 时点、**经 (ts,message) 去重**后，
   **旧解析器口径 12 条 / 修正后口径 15 条** —— 差的那几条正来自「一行含两个 JSON 对象」的 4 行，
   旧解析器会把整行丢掉。更早一次读数（当天早些时候）是 **9 条**：**这个数随时点增长，是时点值**）；
   单侧 / 未达门槛的形状按**形状变化**去重，避免每 10 秒刷屏；
6. `should_rebuild_tunnel` 不再传字面量 `true`：**复核 pid**（探测是异步的，用户可能已经重连换了 pid）。

**换网重建（v0.8.32 的 `spawn_network_watch`）一行未动** —— 既有 7 条 egress 测试与源码守卫全绿。
测试：desktop **162 → 172**（+10）；四向敏感性：(B) 门槛放宽成「任一失败」⇒ 4 条红、
(C) 轮内重试改回 1 次 ⇒ 红、(D) 重建失败一律退直连 ⇒ 红。

### 修复：持续失败时的**日志洪水**（task-91）

实测（用户机器 12:05–12:35，`loglevel: debug`）：**56,292 行 = 31.3 行/秒，峰值 801 行/秒**、379 种「形状」；
其中 `app/dns:` 26,054 行、`failed to retrieve lookup` 11,690、`cache HIT` 8,055
⇒ **洪水主体是 debug/info 记账，不是错误**（错误级只有约 0.04 条/秒）。

* 只在**推给界面**这一路限流（同形状 5s 内 1 条 + **全局 5 行/秒**）；
* **原文照旧完整写入日志文件、照旧进后端环形缓冲**，`connections.observe` 不受影响（拓扑连接统计不被削弱）；
* **不许静默丢弃**：被压下的条数补一条可见摘要
  「核心日志已限流：最近有 N 条未实时显示（原文已完整写入日志文件…）」，
  红线落在 `admitted + accounted == total` 这条断言上。

### 改进：探测目标「成对」（IP 字面量管传输、域名管解析）（task-92）

* 目标从 4 个改为 **3 个、按两类职责配**：`1.1.1.1/`（境外·传输，IP 字面量）、
  `cp.cloudflare.com`（境外·解析）、`223.5.5.5/`（境内·传输，geoip:cn→direct）；
  **移除 `www.baidu.com`** —— 经 SOCKS 多轮实测不稳定（10 轮 4 失败；另一轮 1/10）；
* 原来全是域名，而 `--socks5-hostname` 把域名交给核心解析
  ⇒ **解析一坏就把其实活着的链路判死**，且分不清「传输坏」还是「只是解析坏」
  （活证：同一时刻境内 IP 字面量 404 而境内域名 000）；
* 新增诊断：失败清单**全是域名目标** ⇒ 文案明说「传输路径是通的，更像是解析链路的问题」；
  任一 IP 目标失败时**不许**说这句（有反例测试）。

### 改进 / 清理

* `be1efc8` **绑卡选项按地址族选**：`IP_BOUND_IF` 只对 AF_INET socket 有效，喂给 AF_INET6 在任何状态下都是
  `EINVAL`；v6 要用 `IPV6_BOUND_IF`。现有两个调用方恰好都是 v4 ⇒ 这是**潜伏**错（修前任何 v6 socket 都会 EINVAL）；
* `9cc18cc` **删掉 `probe_helper` 这条死链路**：35 个注册命令里**唯一零引用**；
  删后**重新构建二进制**验证 `strings … | grep -c probe_helper` = **0**；
* `bc39eff` 补上**跨语言命令契约测试**：`generate_handler!` 的注册集 == `ipc.ts` 的 `invoke` 字面量集
  （两侧数量写死 **34 / 34** 做哨兵）⇒ 以后「命令静默消失 / 注册漏了」会**变红**，不再无声；
* `b37bac0` 清理 4 处死代码 / 孤儿资产（`nearestLength`、`.stat*`、3 张截图）；
* `d490935` 新增只读工具 **`scripts/net-metrics.py`**（把 v6 改写 / 探针 / 自愈三组指标固化成一条命令，
  口径写进注释 + `--self-test` 四项敏感性）+ `docs/verification/NET-METRICS.md`；
  `dd95fdb` 修掉它**一行可能含两个 JSON 对象**导致**整行丢失**的缺陷（旧版把「隧道已自动恢复」漏成 0/3）。

### 测试与门禁

`./scripts/check.sh --no-release-build` **exit 0**（在冻结修订 `dd95fdb` 上跑的原始输出与计数见
`docs/verification/RELEASE-v0.8.34.md`）：xt-core **223**、desktop **172**、type_contract **6**、
xt-helper 13、xt-proto 22、xt-tun 71；前端 `vitest` 与站点 6 条版本断言均通过；
`clippy -D warnings` 干净；`check-css-tokens.py` 绿。

### ✅ 给用户的动作：**这一版不需要重装特权助手**

**本版改动全在 App 侧**（`xt-core` 由 App 使用），**更新 App 即生效**。依据是四条命令的实测
（完整输出见 `docs/verification/RELEASE-v0.8.34.md`）：

```
crates/xt-helper/Cargo.toml 的 [dependencies] = xt-proto + xt-tun + serde/serde_json/thiserror/tracing（不含 xt-core）
全仓 grep '^xt-core' --include=Cargo.toml  → 只有 ./Cargo.toml（workspace 定义）与 ./apps/desktop/Cargo.toml
git diff --stat v0.8.33..HEAD -- crates/                → 只动 xt-core/{net.rs, xray/config.rs, store.rs}
git diff --stat v0.8.33..HEAD -- crates/xt-tun crates/xt-proto  → **空**
```

（上一版 v0.8.33 曾含助手侧改动，那时需要重装；**本版不需要**。）

### 已知边界（诚实清单）

* 本文档里的 **before 数字来自用户机器的 `app.jsonl`**（时点值，日志在增长）——
  `a702010` 的口径是「2026-09-22 全天 123,541 行、按 session id 归并」；
  自愈那组同样是**时点值**，口径与上半段**完全一致**：`python3 scripts/net-metrics.py --until 14:21:32`，
  窗口 = **全部记录 → 2026-09-22 14:21:32 +0800**（右端取自附注 tag 的 tagger epoch `1790058092`，
  `git cat-file -p v0.8.34` 可复算）、`app.1.jsonl` + `app.jsonl` 合并并按 `(ts, message)` 去重、
  窗口内 305,742 条 ⇒ **`已作废` 7 次 / `隧道已自动恢复` 3 行**；
  **引用时请连口径与时点一起引**（日志在增长，去掉时点的「当前」等于永远错）；
* **用户装上新版之后的「after」数字现在不存在** —— 要等他更新后才有对照，本文档**不预填、不推测**；
* `NET-METRICS.md` §5 里 **v4-only 408 vs 480 的 +72 未解释**，如实保留；
* 真机安装 / 重启 / Gatekeeper 未在本环境验证；本环境也不在中国大陆，**GFW 行为无法复现**；
* `scripts/check.sh` 的 6 条站点版本断言仍**不覆盖** `Cargo.lock`，也**不覆盖**
  `site/{,en/}wasm/index.html`；
* **站点两阶段发布**：提交 1 阶段官网如实写「正在发布」，**不写死字节数、不给 pinned 直链**。

## 0.8.33

> 这一版修的是「**删除 ≠ 恢复**」：安装路由时把内核原本那条 on-link 路由**顶掉**了，
> 回滚却只删除自己装的那些 ⇒ 留下永久空洞。另加：**助手版本不一致看得见、可一键重装**，
> 以及**切换节点失败后作废「自动重连」意图**。

### 修复：回滚不再「只删除」—— 装之前记账、回滚时**恢复**被顶掉的那条路由（task-85）

**实测的损坏**（本机 2026-09-22，未连接状态）：`route -n get 127.0.0.2` 得到
default / gateway `192.168.0.1` / `en0`；路由表里 `192.168.0 link#14 UCS en0` 在、
而 **`127/8` 那条不在**（其它网卡的 on-link 接口路由都在，唯独 `lo0` 的缺）
⇒ `.1` 以外的回环地址**永久走默认路由**。

**机制**：`route add` 对**同前缀**是**替换**语义 —— 我们装 `127.0.0.0/8 → 物理网关` 时，
把内核那条 on-link 的 `127/8 → lo0` 顶掉了；而回滚只按快照「反序删除自己装的那些」，
**内核原来那条不会自己回来** ⇒ 留下空洞。**「删除 ≠ 恢复」才是根因**
（`63b84dc` 只是让这一个实例不再被装，没有修根因）。

* `xt_proto::InstalledRoute` 新增 `replaced: Option<RouteVia>`（`#[serde(default)]`，
  **老快照照样能反序列化**）；
* **安装前记账**：旁路/host 路由在 `install_route_or_skip`、捕获路由在 `commit_routes_and_dns`，
  装之前都先查同前缀上原本是什么；
* 新增 `route::existing_route()`：`route -n get -net <cidr>`（**实测是精确前缀查找**，
  不是最长前缀 —— 空洞时回 `not in table`）；`ExistingRoute::to_via()` 能把
  「无 gateway 行的接口路由」还原成 `Interface`；
* 新增纯函数 `plan::rollback_plan()` → `Delete` +（**有记录才有的**）`Restore`；
  `controller::rollback` 改为执行它（DNS 仍先还原、顺序仍反序、失败不中断其余项）；
* 8 条新测试：核心断言（原本有接口路由 ⇒ 回滚必须含 Restore）、
  **反例**（原本没有 ⇒ 不得凭空造）、顺序（反序 + 先删后恢复）、老快照兼容、
  `route get` 三种输出解析（接口路由 / 网关路由 / 空洞）。

### 新增：助手版本「三态」看得见 + 指纹缓存（稳态 0 次 spawn）+ 一键重装

**为什么**：App 更新**不会**刷新特权 helper（`restart_helper` 只 kickstart 磁盘上那份**旧**二进制，
只有 `install_helper` 才把包内那份拷过去），而**路由/DNS 的安装与回滚都在 helper 里**
⇒ 用户以为「更新了就拿到全部修复」，helper 侧那部分其实**没上机，且完全无声**（此前只校验协议号）。
实测证据：`9/13` 装的 helper（sha256 `40339c95…`、无 0.8.31）与包内那份
（sha256 `d0f2a262…`、0.8.31）**逐字节不同**。

* **读实际工件**：直接执行两边的 `<binary> version` 子命令（纯打印、不需要 root、
  不连 socket、不碰系统配置，实测 p50 **2.7ms**）。**不用「包内版本 vs App 版本」**
  这种间接推断 —— 那两者本来就会一起变；
* **三态进快照**：`match{version}` / `mismatch{installed,bundled}` /
  `unreadable{installed,bundled,reason}`。「读不到」**如实降级** ——
  **既不许猜成不一致、也不许猜成一致**。**不自动重装**（特权操作必须用户点）；
* **界面**（`fa40424`）：`mismatch` ⇒ `role="status"` 警告「**已安装 X / 随 App 附带 Y**」
  + 「重新安装助手」入口；`match` ⇒ **什么都不显示**（否则狼来了）；
  `unreadable` ⇒ 中性提示「无法核对助手版本：{reason}」；字段缺失（预览快照）⇒
  什么都不显示且不崩。文案说明**为什么**要重装，同时**不夸大**（App 本身已更新，其余修复不受影响）；
* **指纹缓存**（`a111704`）：缓存键 = 二进制指纹 `(存在?, size, mtime秒)`，稳态 **0 次 spawn**
  （只剩两次 `metadata`，实测 `os.stat` p50 **1.0µs**）⇒ 每次快照从 +5.4ms 降到
  **+0.002ms**；**包内那份也用同一套缓存**（而不是「读一次永久缓存」），
  因为「**不许缓存掉『读不到』而不设失效**」——重装后文件出现 ⇒ 指纹变 ⇒ 必须重读并改口。

### 修复：切换节点失败必须作废「自动重连」意图（task-75）

`select_node` 在 `start_core` 失败（且回退也失败 / 无处可退）时只 `return Err`，
盘上 `was_connected` 仍是 `true` ⇒ 隧道断了，**下次启动 / 登录项自启 / 自更新重启会拿
这个刚被证明起不来的节点再接管一次网络**（task-64 修的那条逃生路在这条路径上仍是漏的）。

* 新增 `SwitchEnd{TargetUp, FellBackUp, NoTunnel(FailureExit), TeardownFailed}`
  + 纯判据 `switch_end_keeps_intent()` + 唯一收尾点 `settle_switch()`：
  **拆隧道失败 ⇒ 状态未知，不作废**；无处可退 / 取不到状态 / 落盘失败 / 回退也起不来 ⇒ **作废**；
  回退起来了 ⇒ **保留**（用户仍连着）；目标起来了 ⇒ **保留**；
* 覆盖缺口一并堵上：新增 `FailureExit`（7 个变体）+ 守卫测试
  `every_failure_exit_still_invalidates_intent_in_production_source`
  （按变体在生产源码里逐个计数 == 1 ⇒ **删掉任意一处作废调用就红**）；
  `66fa0e3` 修掉守卫自身「先删注释再计数」的漏洞；
* `core.rs` 里「自动回退节点失败」那个作废点**删除**：`select_node` 现在自己知道隧道最后起没起来，
  由它作废；那里只留日志，且**不再自己解释结果**（旧文案在「回退成功但目标失败」时就是错的）。

### 其他

* `6769b27` 预览快照与真实类型**逐字段对齐**（tun 缺 5 个、helper 缺 `version_check` 等）+ 防回归断言；
* `383cd3d` 把 `auto_reconnect` 收回 TS 声明（撤掉类型旁路），并扫出全部
  「后端有、TS 没有」的字段（19 组契约扫描）。

### 测试与门禁

`./scripts/check.sh --no-release-build`（与 CI 同一套）**exit 0**：前端 **21 个文件 208 passed + 1 todo（209）**
（上一版 18 文件 201 + 1）；Rust **478 passed / 0 failed / 2 ignored**（desktop lib **151**、
xt-core 216、xt-tun **71**、xt-proto 22、helper 13、type_contract 5）；
CSS token 检查绿；站点版本断言 0.8.33。

### ⚠️ 更新后请**重新安装一次特权助手**

| 本版改动 | 在哪一侧 | 生效方式 |
|---|---|---|
| **回滚恢复被顶掉的路由**（`019ab73`） | **助手侧**（`xt-tun` / `xt-proto`，由 helper 执行） | **需重装助手** |
| 助手版本三态、指纹缓存、设置页提示与一键重装 | App 侧 | 更新 App 即生效 |
| 节点切换失败作废自动重连意图、预览快照对齐、TS 契约 | App 侧 | 更新 App 即生效 |

* 本版起 App **会自己提示**「助手版本不一致 → 一键重装」，但**仍然要写在这里** ——
  用户可能不进设置页，而**路由/DNS 的安装与回滚都在助手侧**；
* **不要**理解成「不重装就什么都无效」：其余修复都在 App 侧，**更新即生效**。

### 已知边界（诚实清单）

* **`task-85` 只保证今后不再产生**这类「被顶掉的路由不再恢复」的空洞；
  **已被旧版弄坏的机器不会自愈** —— 本机现在就缺 `127/8 → lo0`（`127.0.0.2` 不可达）。
  用户可**重启**，或执行 `sudo route add -net 127.0.0.0/8 -interface lo0`；
  **界面里的一键修复尚未实现**（另有卡）；
* **真机国内环境 / GFW 行为无法复现**：本环境不在中国大陆；
* 「助手版本三态」依赖能执行两边的 `<binary> version`；读不到时按 `unreadable` **如实降级**，
  既不猜一致也不猜不一致；
* `scripts/check.sh` 的 6 条站点版本断言仍**不覆盖** `Cargo.lock`，也**不覆盖**
  `site/{,en/}wasm/index.html`；
* **站点两阶段发布**：提交 1 阶段官网如实写「正在发布」，**不写死字节数、不给 pinned 直链**。

## 0.8.32

> 修**用户正受影响**的「国内不通、国外正常、有时」：**换网后隧道不再重建**
> （`direct` 出站绑死在连接那一刻的网卡）+ **看门狗只探境外** ⇒ 两条恢复路径都永远不触发。
> 同时修掉「整个 `127.0.0.0/8` 被指向局域网路由器」，并把设置页默认视图缩短。

### 修复：换网后**国内全断、国外正常**（本版核心）

根因（在用户机器上实测 + 读码）：`Supervisor::start` 把**连接那一刻**探测到的网卡写进
`direct` 出站（`runtime/config.json` 实读到 `sockopt.interface = "en0"`），而 `geosite:cn` /
`geoip:cn` 都走 `direct` ⇒ **换网后国内全断、国外正常**（国外走隧道）。
更糟的是两条恢复路径都失效：换网检测 `spawn_network_watch` **检测到了却只写一条日志就返回**
（用户看不到日志，隧道永不重建）；看门狗又**只探境外目标** ⇒ 同样永远不触发。

* **(a) 换网 ⇒ 真的重建**：换网检测改走**与看门狗同一套** `stop + start` 重建
  （`rebuild_tunnel_in_order` 带 seam，调用序列可断言），复用 `recovery` 进度态，
  提示条写明「因为换网」，成功/失败都留可读日志；三道闸门用纯函数
  `should_rebuild_after_egress_change` 表达：**这条隧道还归我 / 用户还想连着 / 没有别的恢复在跑**；
* **(b) 看门狗同时探境内 + 境外**：`watchdog_probe_all` 并行探
  `REQUIRED_PROBE_TARGETS`（境外 `http://cp.cloudflare.com/generate_204` +
  境内 `http://www.baidu.com/`，经 SOCKS 由节点侧解析，不依赖本机 DNS），
  判据 `probe_results_all_alive` = **每个目标都通才算通**，日志**点名**是哪个目标不通；
  沿用 `FAILURES_BEFORE_REBUILD = 2` 的抗抖动策略；
* **(c) 重绑自动完成**：每次 `start` 都会重新探测网卡（`default_route()` → `physical_interface`），
  所以重建即重绑；绑定本身（防环）保留不动；
* **30s 冷却窗口**（防止在抖动网络上反复重建）：**第一次不受影响**（本次运行还没重建过 ⇒ 立刻重建）；
  被挡下时 `warn` 写明「距上次重建仅 Ns（冷却 30s），这一轮不重建；出口仍不同的话冷却过后会自动重建」，
  并且**继续盯着**（不是 `return`）；30s = 看门狗探测间隔(10s)的 3 倍、换网检测间隔(5s)的 6 倍；
  冷却状态取自**已有**的 `runtime.recovery.started_unix`（不新造状态）；墙上时钟往回调时放行；
* 看门狗原来的 `stop_core(..).is_ok() && start_core(..).is_ok()` 统一为
  `rebuild_tunnel_in_order(..).is_ok()` —— **先证明等价**（枚举 stop/start 四种结果，
  断言「结果 == stop_ok && start_ok」且「stop 失败时不调用 start」），`.is_ok()` 语义不变。

测试：本版 desktop lib **136 passed / 1 ignored**（基线 125，+9 换网重建 / 探测 +2 冷却与等价性），
含一条源码级守卫：**删掉换网重建调用或回退成单目标探测即变红**。

### 修复：整个 `127.0.0.0/8` 被指向物理网关

**实测**（用户机器，连接状态下）：回环网段被装成 `127 → 192.168.0.1 UGSc en0`
（新会话又复现一次）。来源是旁路循环里那句「所有 IPv4 旁路网段一律指向物理网关」。

* 内核本来就把 `127.0.0.0/8` 指向 `lo0`，而它比捕获用的 `0.0.0.0/1` **更具体**（/8 > /1）
  ⇒ TUN 本来就卷不走回环，这条旁路**没有必要**；装上反而有害：
  `127.0.0.2` 及以上会被送去局域网路由器，换网后还指向**旧**网关；
* `default_bypass_networks()` **移除 `127.0.0.0/8`**（`::1/128` 同理）；
  `plan.rs::build_plan` 加**两层防御**：`bypass_hosts` 与 `bypass_networks` 里任何**回环目的地一律跳过**
  —— IPC 请求是**数据**不是常量，只改列表挡不住别的调用方；
* 不变量测试：请求里硬带回环也不产生回环路由、**其它旁路网段仍经由物理网关且仍非 critical**、
  捕获路由不受影响（含顺序不变量）。

> ✅ **诚实边界**：这条**解释不了**用户报的「连不上 `127.0.0.1:3080`」——
> 实测 `127.0.0.1/32 → lo0` **一直在路由表里**（最长前缀必胜）、系统代理全关；
> 受影响的是 `.2` 及以上。

### 改进：设置页 TUN 高级参数收进「高级」折叠

口径：headless Chrome 窗口 1280×900 ⇒ 视口 1280×813，`?preview=1&state=connected&view=settings`
（默认落「连接」分类），量当前分类正文容器（`.set__body`）的渲染高度。

| | 改前 | 改后 |
|---|---|---|
| **「连接」分类** | **1158px** | **950px** |
| └ set-tun | **480** | **271** |

* **进折叠**：隧道网段（默认 `198.18.0.1/15`）、MTU（默认 1500）、IPv6 处理、出站绑定接口
  —— 都是「默认值几乎总是对、改了才需要懂」的值旋钮；
* **刻意留明面**：**哨兵 DNS**（它的说明是「隧道没了而 DNS 还指着它 ⇒ 用户表现为全网断」这条风险的
  **唯一解释**，默认看不见就把已知风险变成暗知识）、**`bypass_private`**（决定家里 NAS / 局域网设备
  还能不能直连，是网络出问题时最先要确认的**自救项**）；
* 取舍：把哨兵 DNS 也收进去是 **840px**，按「哨兵 DNS 说明必须可见」选 950px（两个数都实测过）；
* 用原生 `<details>`（复用 `Routing.tsx` 的 `page__details` 范式），**没新造机制、没加 CSS**。

### 官网：打错的路径不再返回「200 + 首页」

Cloudflare Pages 在**没有顶层 `404.html`** 时，对任何未匹配路径返回 `200 + index.html`
（实测 apex 上 6 条未知路径全是 `200` + `text/html` + body 与首页逐字节相同；GH Pages 镜像是真 404）。
两个后果：**SEO 软 404**，以及**任何「用状态码判断资产存在」的脚本都会被骗**。

* 现在 `site/404.html`（`noindex`、自包含、不写版本号）让未知路径返回**真 404**；
* 附带 `scripts/verify-live-site.sh`：线上资产存在性改用 **content-type + body sha256 双判**；
* **残留（如实说）**：`https://xraytun.top/og-image-0.8.30.png` 仍返回 200 —— 那是
  `_headers` 的 `/og-image*.png → max-age=31536000, immutable` 命中了部署前缓存的软 404 响应；
  **源站已经是 404**（带 cache-buster 实测 `cf-cache-status: BYPASS` + 404），
  清除它需要在 Cloudflare 侧 **Purge 缓存**。

### 工具：断网取证脚本扩展

`scripts/diagnose-network-drop.sh`：回环可用性检查（**含 `127.x.x.x` 非 `.1`**）、
多栈共存（Tailscale / Karing 等）识别、国内外**即时对照**、`--self-test` 双向敏感性；
单次采样改为 **3 轮判据**（避免一次抖动当成结论）。

### ⚠️ 更新后请**重新安装一次特权助手**（本版含助手侧改动）

这一版**不是全部**都要重装 —— 按下面的分工看：

| 本版改动 | 在哪一侧 | 生效方式 |
|---|---|---|
| 换网后重建隧道 / 看门狗探境内 / 30s 冷却窗口 | **App 侧** | **更新 App 即生效** |
| `127/8` 移出旁路列表 | **App 侧**（列表随请求下发，`build_tun_request`） | **更新 App 即生效** |
| `plan.rs`「回环绝不经网关」**防御层** | **助手侧**（`crates/xt-helper` 依赖 `xt-tun`，用 `xt_tun::plan::TunPlan`） | **需重装助手** |

**为什么必须单独说这一句**：更新 App **不会**刷新已经装好的助手 ——
`install_helper` 才用 `/usr/bin/install -o root -g wheel -m 0755` 把**新二进制**拷进去；
`restart_helper` 只是 `launchctl kickstart -k`（或 bootstrap 已有的 plist），**不拷贝二进制**。
助手的版本号只在「设置 → 特权助手」里显示（仪表盘也显示），**App 不做版本比较、不会提示助手过旧**。

⇒ 建议：**更新后到「设置 → 特权助手」点一次「重新安装」**。
⇒ 但**不要**理解成「不重装就什么都无效」：本版**主修复在 App 侧**，重装只是为了补齐助手侧那一层防御。

### 测试与门禁

`./scripts/check.sh --no-release-build`（与 CI 同一套）**exit 0**：

* 前端 **18 个文件 201 passed + 1 todo（202）**（上一版 197 + 1）；
* Rust **455 passed / 0 failed / 2 ignored**：desktop lib **136**（+11，含 `1 ignored`）、
  xt-core **216**、xt-tun **63**（+3）、xt-proto **22**（+1）、helper 13、type_contract 5；
* CSS token 定义性检查绿；「站点声明的版本与 Cargo.toml 一致：**0.8.32**」；
* clippy `-D warnings` 干净。

### 已知边界（诚实清单）

* **真机国内环境 / GFW 行为无法复现**：本环境不在中国大陆；「换网后国内断」的现场来自**用户机器**的
  实测读数与路由表，本机只能做单元/调用序列断言；
* 「看门狗同时探境内」意味着**判据依赖境内目标可达**：若境内目标本身在该用户网络下不可达，
  看门狗会把隧道判为异常并触发重建（30s 冷却兜底）—— 这是刻意的取向（宁可重建，也不让「国内全断」
  悄悄持续），但它**不是**「节点一定有问题」的证据；
* `127/8` 那条**解释不了**「连不上 `127.0.0.1:3080`」（实测 `127.0.0.1/32 → lo0` 一直在表里）；
* 设置页的两个高度数字来自 **headless Chrome**，不是真机 WKWebView；
* `scripts/check.sh` 的 6 条站点版本断言仍**不覆盖** `Cargo.lock`，也**不覆盖**
  `site/{,en/}wasm/index.html` —— 这两处本次仍靠人工核对 + 独立复验；
* **站点两阶段发布**：提交 1 阶段官网如实写「正在发布」，**不写死字节数、不给 pinned 直链**。

## 0.8.31

> 这一版只有一个主题：**让界面说的和实际发生的对得上**。修的是 5 类「静默不作声」的
> 真缺陷 —— **回滚失败被丢弃** / **自动重连意图复活** / **「连上了但已经不通」的窗口看不见** /
> **日志每来一行重建全部 1500 行** / **未定义 token 静默失效**，外加一批性能与可维护性欠账。

### 修复：helper 回滚失败被丢弃 —— 界面写「已停止」，而网络可能没恢复（task-62）

原来的 `stop_core` **先**清 `running` / `pid` / `tun_session`，**再**看 helper 的返回结果。
于是「helper 回滚系统网络配置失败」这条路也被渲染成「已停止」：状态先被抹掉，
失败证据无处可查，用户看到的就是一句办完了的话。

* 新增纯函数 `runtime_after_stop()`：**成功才清** `tun_session`；失败**保留**它
  （那是「helper 上会话可能还活着」的**唯一**证据）并写 `last_error`；
* 新增 `stop_log_line()`：失败时只说「**未能确认网络已恢复**」；
* 看门狗回退分支不再 `let _ = stop_core(...)`：新增 `FallbackOutcome`，
  回滚成功说「网络配置已回滚」（有证据），失败说「未能确认网络已恢复」并给出「修复网络」；
* **对外文案里不再出现「网络可用」这一未经验证的断言**。

### 修复：「退出应用」不再是持久逃生路（task-64 a/c）

`should_auto_reconnect` 读的是持久化的 `settings.was_connected`，而它**以前只有用户亲手点
「断开」才会清**。自动退场那条路（退出应用、自更新重启、登录项自启）从来不写回 ——
磁盘上留着 `true`，下次启动就会拿**同一个刚被证明不通的节点**再接管一次网络：
用户「退出应用」好不容易恢复的网络，又被拆一遍。

* 只读证据：结构化持久状态只有 `settings` / `subscriptions` / `nodes` / `runtime-config` 四份，
  `runtime.recovery.last_outcome` / `last_error` / 提示条**全在内存里、重启即失** ——
  所以判据只能写进那个**已存在**的字段，不新造状态；
* 新增 `IntentDrop{UserStop, KnownFailure}` 与 `connect_intent_after_stop()`（判据）；
* 顺带把「自动连回来」做成**看得见、关得掉**，并钉住「用户关掉后不会被翻回 `true`」。

### 修复：「连接着但已经不通」的 10–20 秒窗口在界面上不存在（task-60 / task-68）

看门狗 10 秒一跳、**连续 2 次**探测失败才重建 ⇒ 约 **10–20 秒**里，界面与健康态**逐字相同**：
绿点 + `已连接 · 香港 · 53 ms` + 「断开」。用户在这段窗口里开始上不了网，却看到「一切正常」，
于是去查路由器、运营商、节点 —— 这是「用户不知道要先断开」最直接的成因。

* `recoveryView()` 纳入 `probe_failures`，新增 `degraded` 态：`隧道探测失败 N 次 ·
  整机断网时先点「断开」` + 完整自救句（断开会走 helper 回滚系统网络配置）；
* **刻意不改成告警色**：状态确实**没有变**，把设计内的窗口染成故障是另一种假陈述；
* 同屏矛盾一并修掉：恢复期间顶栏禁用「连接」而仪表盘可点（现在两处一致）；
  系统代理模式不再写「隧道已建立」「默认路由尚未接管」这种自相矛盾的话。

### 修复：日志「关闭跟随」后仍一直跳动（`ee602d9`）

用户**第二次**报同一现象，这次找到主因，与上一轮那条路径是**叠加**关系：

* `36335b4` 修的是 `useFollowScroll` 把显式关闭的「跟随」悄悄设回开 —— 真 bug，
  但**只影响自动滚动这一条路径**；
* **本次修的**：`Logs.tsx` 的 key 是 `` `${ts_unix}-${i}` `` —— **下标进了 key**，
  而缓冲满员（`MAX_UI_LOGS=1500`）后每来一行就从**前面**裁一行 ⇒ 所有下标整体前移 ⇒
  **所有 key 变化 ⇒ React 卸载重建全部 1500 行**。它与跟随开关**完全无关**，
  所以**关掉跟随也照样跳**；核心 stdout 持续转发 ⇒ 每行都发生。
* 改为稳定身份：`LogEntry` 加 UI 层单调唯一的 `seq`（实时 `onLog` 与历史 `tailLogs`
  两条入口统一分配），`key={line.seq}`。

### 修复：设置页选中标签的描边用了**未定义 token**（task-61）

`--border-interactive` 在**应用侧未定义**（它只在**官网**的 CSS 里有定义 —— 典型的
**跨 bundle 泄漏**）：`var()` 引用一个没有值的自定义属性、又没有兜底值，这条声明会在
**计算值阶段**失效，`border-color` 落到 initial 值 `currentColor` ⇒ 选中态多出一圈
计划外的**近白描边**（实测计算值 `rgb(230,236,245)`，与背景对比度 14.19:1）。
浏览器不报错、样式表不报错、`tsc` 不报错 —— 只有人拿放大镜量计算样式才看得出来。

### 新增：一道能防住这一族的检查（task-57）

这是本项目**第三次**出现「静默失效的声明」（前两次：`--line` 从未定义导致拓扑分隔线
从 v0.8.24 起一直没渲染；`xattr -dr` + `2>/dev/null` 把必然失败吞掉）。三次的共同点都是
**没有任何检查会发现它**，所以加了 `scripts/check-css-tokens.py` 并接进 `check.sh`：

* **按 bundle 判定**（应用 UI / 官网各自结算）——这是本检查最关键的一点：
  `--border-interactive` 在**整个仓库里搜得到**，用「全仓库定义集合」去判它**永远测不出来**；
* 只把 `var(--x)`（**不带兜底值**）算违规；`var(--x, fallback)` 合法；
* 当前基线：**应用 UI 23 个 token / 273 处 `var()`**（2 处带兜底）、
  **官网 41 个 / 87 处**（0 处带兜底），违规 **0**。

### 性能

* **拓扑每帧 `querySelector("rect")` × 车数 → 按 key 缓存**：实测 **662/s → 2/s**（task-63）。
  按车数与帧率算，这是每帧几十次 DOM 查询的固定开销；
* **门禁的两个探测改为并行**（`JoinSet`）：最坏 **12s → ≤6s**，语义不变
  （仍要求每个目标都拿到真实响应才 commit，task-42 的 4 条反例保持绿）；
* 补**逐阶段计时**：`tun_up_and_fd` / `wait_for_port` / `pre_commit_gate` / `commit_routes` /
  `stop_core` 各记一条 `tracing info(ms)`。

### 改进：未运行时点「模式」不再隐式连接（task-54）

以前 `if mode != Direct { start_core }` 没有 gate —— 「改一个偏好」被实现成「完整连接」
（最坏 30s+）。现在核心没在跑时 `set_mode` **只持久化偏好**，日志写明「当前未连接，点连接开始」；
运行中仍然停机 + 重启（模式变了，路由与 DNS 必须重建，这段是必要的）。

### 其他订正与清理

* `1a1c8a3`（task-57 第三节）：删掉 **190 行逐字节重复**的 CSS
  （`copy2 = 2346-2535` 与 `copy1 = 2116-2345`，逐段 `diff` 无输出）；删掉**永不触发**的
  880px 断点；补 `prefers-reduced-motion`；修 `.row--between`；
* `1fa48ee`（task-67）：文案不再把因果**唯一**归到节点；
* `0bd6060`：设置页三处破坏性操作补确认；摘掉一个「点了没用」的开关；
* `035e083`（task-72）：「未设系统代理」徽章只在运行时显示；同屏两个「断开」收敛为一个；
* `b05d1f3`（task-73）：拓扑图例第 4 项「内部通道」描述的不是线，移到它真正出现的那组标题旁；
* `2dddfa9`：修掉一条由「时间戳跨秒」造成的**偶发红**断言（不是产品缺陷，但会让门禁说谎）。

### 已知边界（诚实清单）

* **真机国内环境 / GFW 行为无法复现**：本环境不在中国大陆，只能保证「门禁不通过时不接管默认路由」；
* 「退出应用后不再自动重连」的判据、`IntentDrop` 与单元测试已落地，但
  **登录项自启 / 自更新重启这条完整回路未在真机上复现**（需要真实登录项 + 自更新回环）——
  如实记为本轮结论到**代码级 + 单测**为止；
* 拓扑 **662/s → 2/s** 是**对 `querySelector` 调用次数的计数**（headless Chrome + 计数钩子），
  **不是 Instruments 时间线**；帧率是否同样改善**未单独测量**；
* `scripts/check-css-tokens.py` 有三条**故意不处理**的局限（写在脚本头部）：只在 `@media` 里
  定义的 token 会被算作已定义 / 字符串里的 `var(--x)` 会误报 / 不检查「定义了但没人用」。
  当前仓库这三种情形都已核过不存在；
* `scripts/check.sh` 的 **6 条站点版本断言不覆盖** `Cargo.lock`，也**不覆盖**
  `site/wasm/index.html` 与 `site/en/wasm/index.html` —— 本次这两处靠**人工核对 + 独立复验**，
  这是已知的覆盖盲区，不是「断言通过 ⇒ 全都同步了」；
* **站点两阶段发布**（v0.8.29 起的规矩，见下）：提交 1 阶段官网如实写「**正在发布**」，
  **不写死字节数、不给 pinned 直链**（那时资产还不存在）；真实字节数与直链在提交 2 填入。

### 测试与门禁

* `./scripts/check.sh --no-release-build`（与 CI 同一套）**exit 0**：
  前端 **18 个文件 197 passed + 1 todo**；Rust **440 passed / 0 failed / 2 ignored**
  （desktop lib 125、xt-core 216、xt-tun 60、xt-proto 21、helper 13、type_contract 5）；
* 新增的 CSS token 检查接进门禁后为绿：应用 UI **23 token / 273 处 `var()`**（2 处带兜底）、
  官网 **41 / 87**（0 处带兜底）；
* 门禁两个探测并行后在「无节点 / 失败路径」条件下实测 **152ms**（`c62a4ae`）。

## 0.8.30

> 把用户逐条实测到的缺陷修完：**拓扑回流** / **蓝车漂移** / 更新按钮 / 状态线 /
> 关闭跟随 / 设置页层级 / 自动更新去隔离标记 —— 外加一批「陈述比事实更强」的订正。

### 修复：第三个出口永远没有车（拓扑「回流」）

真浏览器 DOM 实测段序（guide 总长 **2324.9px**）：

```
主干[0,169.4]  去₁[169.4,484]    回₁[484,798.6]    去₂[798.6,1123.7]
回₂[1123.7,1448.8]  去₃[1448.8,1802.1]  回₃[1802.1,2155.5]  回主干[2155.5,2324.9]
```

老代码用「弦长 ≤ 弧长」把每圈跨度限制在 `outboundLen ≤ 1331.9`，而 **去₃ 的起点是 1448.8**
—— 车**永远到不了第三个出口**（算术可证，与弦长具体值无关）；而且它沿出口 1 的原路
**倒着走回去**，看起来就是「回流」。

现在一圈 = **主干 + 一条分支**，**绝不走回程段**。
`guide_to_visible` 实测仍是 **0% / 0px** —— B1 那次修复完好，问题只在「车走哪些段」。

### 修复：「蓝车漂移」

tester 先取证量化（`docs/ui/topology/DRIFT-EVIDENCE.md`）：

| 量 | 实测 |
|---|---|
| 渲染位置离线（max） | **33.85px** |
| 离线 >1.5px 占比 | **19.12%** |
| 滑行帧离线 vs 正常行走帧离线 | **48.14% vs 3.11%** |
| 按出口（node/direct/block） | 33.85 / 18.21 / 9.67 |
| guide → 可见线（对照） | 0.0025px（正常） |

根因：限速器做的是**屏幕空间线性插值**，而**每圈回绕那一次 ≈486px 的跨度**也走同一条路径
→ 每圈花约 **2.7 秒**沿一条直线飞回入口，用户看到的就是「漂移」。

现在**回绕那一帧瞬时落位**（跳过屏幕插值，直接落到 `P(arc)`）并配 140ms 淡入；
**几何变化时仍保留滑行**（那是需要看到的过渡）。

### 修复：已是最新版仍显示「更新到 X 并重启」

后端早就在发 `app_update_available`，但前端**完全没用它** —— 按钮门槛只判断
「查到了没有」（`latest_app &&`）。于是只要成功检查过一次更新，无论你是不是最新版，
按钮永远在。现在门槛是 `app_update_available && latest_app`，并在
「查到了 + 确实没有新版 + 没有 `check_error`」时给一句「已是最新版本」回执。

### 修复：顶栏绿线不跟状态（直连/系统代理不再谎报「已受保护」）

`.topbar` 的底边**写死**绿色，而绿色在本项目语义里 = 已受保护。于是直连模式下等于谎报
「你受保护了」（实际毫无代理）；自动兜底退回直连时更严重：**流量已在裸奔，界面还在报平安**。

现在 5 个 tone 由**后端真实字段**判定，绿色**只属于** on（TUN + running + `routes_committed`）；
并且把状态语义提成**唯一真源**（`topbarStatus.ts::appStatus()`），顺带修掉仪表盘在系统代理
模式下写「**隧道已建立**」「默认路由尚未接管」的自相矛盾 —— 原来是同一件事在两处各写一份判断。

### 修复：关闭「跟随」后日志还在动

`useFollowScroll` 的 `onScroll` 与复选框**共用同一个 state**，因此分不清
「用户滚回底部」（位置意图 → 该恢复跟随）与「用户显式取消跟随」（明确指令 → 必须保持关）。
现在**显式意图压过位置驱动**。

### 改进：设置页两级结构（选中哪一类只显示那一类）

改前 10 个分节**全部同时渲染**（平铺 779 行），顶部导航只是 4 个锚点 —— 内容层没收敛，
「开机自启动」埋在 `top≈2997px` 处根本找不到。

现在 **4 类 / 10 分节**，选中某类时**只挂载该类**的分节：其余**不在 DOM 里**
（不是 CSS 藏起来 —— 藏起来的东西仍在无障碍树里、也能被 Tab 聚焦）。
导航改为 `role="tablist"` / `role="tab"`：`aria-selected` + 游走 `tabIndex` +
←/→/Home/End，选中态用内嵌竖条 + 加粗（不只靠颜色）。
需要处理的状态用**徽标**（`--warn` 圆点 **加上** `aria-label`）提示，而不是自动切类别
偷换用户正在看的内容。

顺带修「去处理」跳设置页时**不带目标分节**会落错分类：helper 缺失时现在直接带到 `set-helper`。

### 修复：自动更新去隔离标记

原命令是 `/usr/bin/xattr -dr com.apple.quarantine <target> 2>/dev/null || true`：

* `-r` 在不支持它的实现/版本上 **exit 64**；
* `2>/dev/null || true` 把失败**完全吞掉** → 隔离标记从未去掉，而 `app-update.log` 里**没有任何线索**
  （用户更新后被 Gatekeeper 拦，却查不出原因）。

现在改为 `find <target> -exec /usr/bin/xattr -d com.apple.quarantine {} + 2>&1`，
并**加读回验证**：去掉之后再读一次，确认标记真的没了。

### 订正：v0.8.27 条目里关于 `xattr -r` 的说法不准确

v0.8.27 写的是「现在的 macOS 里 `xattr` **根本没有 `-r`**」。实测（macOS 26.6.2 / build 25G83）：

```
$ which -a xattr
/Library/Frameworks/Python.framework/Versions/3.12/bin/xattr   ← Python 的 xattr 包
/usr/local/bin/xattr                                            ← 同一个包
/usr/bin/xattr                                                  ← Apple 的（Mach-O）
$ xattr -dr com.apple.quarantine /tmp/xtest          → exit 64「option -r not recognized」
$ /usr/bin/xattr -dr com.apple.quarantine /tmp/xtest → exit 0，标记真的被删掉
$ /usr/bin/xattr -h → usage: xattr [-l] [-r] [-s] [-v] [-x] file …   ← 有 -r
```

准确的说法是：**`xattr` 有两个不同实现，`-r` 的支持随实现与版本而异**。
**历史条目保留原样**（不改过去），在此更正。README / release.yml / 脚本里的散文已按此改写，
并统一口径：

* **产品脚本**：一律写绝对路径 `/usr/bin/xattr`、**不要依赖 `-r`**（要递归用 `find … -exec … +`）、
  **不要 `2>/dev/null`**（失败要留痕）；
* **给用户手动执行的安装指引**：可以保留 `2>/dev/null` —— 它只压掉 `No such xattr` 这类刷屏噪声，
  不影响判断成败。

### 其他订正

一批「**陈述比事实更强**」的表述按实测改齐（官网 + 正本 + README/release.yml + 脚本散文）。

### 已知边界（诚实清单）

* **真机国内环境 / GFW 行为无法复现**：本环境不在中国大陆，只能保证「门禁不通过时不接管默认路由」；
* **「门禁在接管默认路由之前能否跑通」是代码级推理**，未在真机实测；
* **拓扑漂移的证据来自 `?preview=1` + headless Chrome，不是真机 WKWebView**；
* 本机 `codesign --verify --strict` 复跑取决于能否下载到 dmg（下载不到会如实写「未完成」）。

## 0.8.29

> 修**用户真实撞到**的「国内网络会断掉」：**接管默认路由之前**新增一道端到端门禁，
> 门禁不过就**不接管**，系统网络保持原样。

### 修复：接管默认路由前的端到端门禁

用户症状是「连着就整台 Mac 断网，一断开/退出就恢复；国外可以，国内直接断掉」。

根因是**门禁不足**：原先两道检查只验

1. 本机 SOCKS 端口在**监听**；
2. 到节点的**直连 TCP** 可达。

而国内最常见的情形是 **TCP 三次握手能过、但代理协议握手被墙**（`nc -z` 成功、
真实请求失败）。这两道检查都通过 → 应用就接管了 `0.0.0.0/1` + `128.0.0.0/1`
两条默认路由并改 DNS → 之后**所有**流量（包括本来能直连的国内流量）都被送进这条
已经不通的隧道 → **整机断网**。

现在在 `deferred_commit` 分支里、两次 TCP 检查之后、`CommitRoutes` **之前**，
增加一道**经本机 SOCKS 发真实 HTTP 请求**的门禁：

* **两个目标必须都通**：一个**境外**目标（走代理链路）+ 一个**境内**目标
  （`www.baidu.com`，命中 CN 分流、走 direct 出站）。
  只探境外会让「国内断、国外正常」这种症状**恒通过**，门禁就形同虚设；
* 单次探测超时 **6 秒**；任一目标拿不到真实 HTTP 响应 → **不接管默认路由**，
  回滚 TUN 并报错：

  > 已在接管默认路由之前中止，系统网络未被改动。请换一个节点后重试。

* 探测目标为空时 **fail-safe**：拒绝接管（按配置错误处理，不当成网络问题）；
* 失败的两种情形分开归类：**门禁没通过** vs **commit 自身失败**，
  文案不同，不把两种原因混成一句。

**代价**：启动比之前多约 **2–3 秒**（最坏情况 12 秒 = 2 个目标 × 6 秒超时）。

**实现侧**：探测只有一个实现（`supervisor.rs::socks_http_probe`），
`commands/core.rs` 的 `tunnel_probe` 变成薄封装 —— 不再有第二份 curl 调用。

### 测试

* `cargo test -p xraytun-desktop` 覆盖 8 条门禁测试（含**调用序列断言**）：
  两目标皆通才 `commit`；境内黑洞 / 境外黑洞 / 超时空码 → 均**不得**调用 `commit`；
  `commit` 自身失败单独归类；空目标 fail-safe；响应码判定；目标组合固定为境外 + 境内。

### 已知边界（诚实清单）

1. **「门禁在接管默认路由之前真的能跑通」是代码级推理，未在真机 / 国内环境实测。**
   若实机证明这条门禁跑不通（例如探测本身依赖尚未就绪的链路），
   替代方案是「commit 之后、返回成功之前判定，失败即回滚」。
2. **本环境不在国内，无法复现 GFW 行为** —— 我们无法验证「国内 TCP 通、协议被墙」
   在真实网络下的表现，只能保证**门禁不通过时不接管**。
3. **门禁只覆盖 TUN 两阶段（`deferred_commit`）路径**；
   单阶段启动与系统代理模式**没有**这道门禁（那两条路径不改默认路由，风险不同）。

### 维护者必读：站点的两阶段发布

本站下载区采用**两阶段**（`gen-site-geo.py` / `gen-site-jsonld.py` 里的 `PUBLISHED` 开关）：

* **提交 1（本次）**：只写「v0.8.29 正在发布，下载见 Releases 页面」——
  因为此刻资产还不存在，**不能**给 pinned 直链（会 404），也**不能**沿用上一版字节数；
* **发版成功后必须做提交 2**：`gh release view v0.8.29 --json assets` 取**真实**字节数
  → 把两个生成器里的 `PUBLISHED` 置 **`True`** 并填入真值 → 重跑三个生成器 → 推送。

**忘记提交 2 的表现**：站点会一直停在「正在发布」—— 不报错，但用户拿不到下载直链。
所以「置 `PUBLISHED=True`」是发版流程的**必做步骤**，不是可选收尾。

## 0.8.28

> 修一个**用户真实撞到**的 TUN 缺陷：会话泄漏后应用**自己清理并重试一次**，
> 不再要求用户手工 `tun_down`。

### 修复：会话泄漏后不再要求用户手工 `tun_down`

用户在真实使用中撞到的是这句：

```
helper 建立 TUN 失败：已有活跃会话 s1789903818854972000（接口 ut6），请先 tun_down
```

根因是 `tun_up` 在已有会话时发的是**通用** `InvalidRequest`，桌面端因此无法把
「这条错误值得自愈」与「路径白名单失败」这类**同样返回 `InvalidRequest`** 的错误区分开。

现在：

* `xt-proto` 新增结构化错误码 `ErrorCode::SessionConflict`（线上名 `session_conflict`，
  由测试钉住）。判据**只此一处** —— 在 helper 与桌面端各写一份字符串匹配必然漂移，
  而漂移的表现是「自愈在某些用户机器上静默失效」；
* 撞上会话冲突时：记 `warn` 日志 → 发 `Request::Restore` 强制拆掉泄漏会话 →
  **重试恰好一次**；一次就成功时不发任何多余请求；
* 仍失败时的文案写明**已经做过什么**：三种失败形态各自说明
  （「已自动清理并重试一次」/「清理本身失败」）。

**触发条件刻意收窄**：只认 `is_session_conflict()`。路径白名单失败、helper 未安装、
权限不足等一律**原样上报** —— 这类「自动修复」最常见的事故，就是把自愈做太宽、
把本该暴露给用户的真实错误吞掉。

**为什么要「先 Restore 再重试」**：被拆的那条会话必然**没人负责** —— 若它还有主人，
helper 的 `tun_up` 本来就会把「另一个进程的合法会话」判成冲突并拒绝；而且 `Restore`
正是启动期清理遗留物所用的同一条路径（`tear_down_live_session` +
`controller::force_cleanup`）。

**兼容旧 helper（不是过渡态）**：helper 是单独安装的特权二进制（装一次要管理员密码），
「新 App + 旧 helper」会长期共存。所以保留了「旧 helper 的 `InvalidRequest` +
『已有活跃会话』这一句」的兼容分支 —— 没有它，自愈对**已经装过 helper 的用户**直接失效。

### 测试

* `cargo test -p xraytun-desktop -p xt-proto -p xt-helper` → **93 + 21 + 13 全绿**；
* 自愈主链路断言**真实调用序列**（`tun_up` → `restore` → `tun_up`），不只看最终返回值；
* 关键反例：`InvalidRequest` 但消息**不含**该片段（路径白名单失败）→ **不**算冲突；
  错误码不对（`Unauthorized` / `Internal`）即使消息带那句话 → 也不算冲突；
* 敏感性用**变异验证**（改在 `/tmp` 的副本上做，已精确还原）：把判据放宽成
  「所有错误都自愈」→ 反例变红；删掉旧 helper 兼容分支 → 兼容测试变红。

### 已知边界（诚实清单）

* **「运行期间泄漏」的真机时序没有复现**：那是一处竞态，本机没有在真机上重造出
  「连接中途失败留下会话 / 上个进程未干净退出」的时序。自愈路径由单测覆盖
  （含注入假实现，覆盖「第一次冲突、第二次成功」），但没有真机端到端跑过这条竞态。
* macOS 真机 GUI 无法自动化验证（helper 安装的管理员密码弹窗、系统设置里的放行流程）。
* 连接与域名的配对是**时序近似**（`accepted` 行不带连接 ID）。
* TUN 建卡 / 改路由 / 回滚没有在真机上跑完一次「下载 → 安装 → 连上 → 退出回滚」全程。

## 0.8.27

> 自动恢复的过程在界面上可见了；官网（中文 + 英文）上线；**安装说明修正**
> —— 原来那段照做是装不上的。

### 新增：自动恢复过程在界面上可见

看门狗重连核心时，界面以前把「正在自动恢复」显示成「未连接」——用户会以为
必须自己点一次连接。现在：

* 顶栏与仪表盘显示「正在自动恢复（第 N 次）」，连接按钮在此期间为
  「正在恢复…」且 `disabled=true`；
* 成功后回到「已连接」，并给一次性提示「已自动恢复连接（第 N 次）」；
* 失败时如实显示「自动恢复失败」，按钮恢复可点（**不假装已连上**）；
* 修掉「已自动恢复」与「恢复失败」同屏矛盾的残影（CDP 实测发现）；
* 新增 11 条单测锁住 5 条界面不变量（recovering 时按钮绝非「连接」、
  文案不退化成「未连接」、成功后不留残影、失败要说直连且按钮可点、
  没有恢复信息时什么都不说）。

### 新增：官网（中文 + 英文）

`site/` 下的纯静态站：不用执行 JavaScript 就能读到全部关键内容（下载、安装、
FAQ 都在原始 HTML 里），另有给 AI 检索用的 `llms.txt` / `llms-full.txt`、
`robots.txt`（13 个 User-agent 块、0 条 Disallow）与 `sitemap.xml`（2 个 URL、
三向 hreflang 互指）。已用 **GitHub Pages** 发布
（`.github/workflows/pages.yml`），并对 **project 站点的子路径**
（`harodggg.github.io/xrayTun/`）逐条实测：站内链接与资源（含 `/en/` 与 CSS 里的
`url()`）全部 200；远端内容与仓库文件 sha256 逐个一致。

### 修复：安装说明照做会失败（用户装不上）

原来 README 与 Release Notes 都写「右键 →「打开」，或 `xattr -dr com.apple.quarantine …`」。
实测两处都不成立：

* 现在的 macOS 里 `xattr` **根本没有 `-r`**：`xattr -dr` 与 `xattr -cr` 都以
  `option -r not recognized`（exit 64）失败 —— 看起来像清掉了，其实没有；
* 只给 bundle 根路径的 `xattr -d com.apple.quarantine /Applications/XrayTun.app`
  **只清掉根上那一个**：实测 13 个带 quarantine 的文件里还剩 **12 个**；
* 「右键 →「打开」」**已被 Apple 在 macOS 15 移除**（2024-08-06 公告）。

现在按版本给三条可执行路径：macOS 15+ 用「系统设置 → 隐私与安全性 → 仍要打开」、
macOS 14 及更早右键「打开」、终端用
`find /Applications/XrayTun.app -exec xattr -d com.apple.quarantine {} +` 逐文件清；
并写明**核心随包附带（`Contents/Resources/{xray, geoip.dat, geosite.dat}`），
不需要自己安装 Xray**。

### 修复：`codesign --verify --strict` 失败（会挡住将来的公证）

发布的 App 里 **13 个文件**带 `com.apple.FinderInfo`，而 CI 用的是**非严格**的
`codesign --verify --verbose=1`，所以一直显示成功。根因是 `hdiutil makehybrid -hfs`
**会给镜像里每个文件补 FinderInfo** —— 用零 xattr 的干净源目录做对照实验也一样，
而 `ditto` 打的 zip 不会。修法四步：① 签名前逐文件清；② dmg 生成后在可写镜像里
再清一次（`makehybrid → UDRW → 挂载 → 清 → UDZO`）；③ 交付前断言镜像内 App 必须
通过 `--strict`；④ CI 改 `--strict`（清不掉时 CI 直接失败，不静默放行）。

### 修复：几处「陈述与事实不符」

* **设置页 5 张卡锚点不可达**：`id` 重复（`set-dns`×3 / `set-sys`×2 / `set-core`×2 /
  `set-conn`×2）→ 9 张卡各有唯一 id，4 个导航锚点实测跳转正确；
* **地球仪**「飞机数量与快慢由实测速率决定」与实现不符：数量来自**累计流量**
  （跨核心重启单调），快慢是固定视觉节奏；
* **未定义 token `--line` 且无兜底** → `.highway__internal` 那条虚线分隔线
  **从 v0.8.24 起一直没渲染出来**；同时确认 `--panel` 有兜底、渲染正常，
  所以只修前者（不把「有降级路径」当 bug 修）。

### 改进：「开机自启动」不再埋在折叠线下

用户头号需求是「开机后自动连上、不用点连接」，而那个开关原先埋在「其他」卡最底下：
1080×720 实测控件 `top=2997px`，完全在折叠线以下。现在独立成卡放在「代理入口」之后，
实测 `top=402`（控件 `top=457`）—— **不滚动即完整可见**。

### 其他

* 补 MIT `LICENSE`（与 `Cargo.toml` 的 `license = "MIT"` 名实相符）；第三方组件声明
  （Xray-core MPL-2.0、geoip/geosite）单独放 `THIRD-PARTY.md` —— 这样 GitHub 才能把
  `LICENSE` 正确识别为 MIT，而不是 `Other`；
* 跨语言契约测试覆盖 `connection` 系列与 `RecoveryState`；
* 官网与文档中若干与实现不一致的表述按事实修正。

### 已知边界（诚实清单）

* **macOS 真机 GUI 无法自动化验证**：系统设置里的「仍要打开」、helper 安装的管理员
  密码弹窗都只能人工点；本机无图形会话，未实测这两步。
* **连接与域名的配对是时序近似**：`accepted` 行不带连接 ID，域名靠「时间最近的前一条
  `sniffed`」配对；配对覆盖率与真实值未在真机统计。
* **未跑完整真机端到端**：TUN 模式建卡/改路由/回滚只走了单测与协议层测试，
  没有在真机上完成一次「下载 → 安装 → 连上 → 退出回滚」的全程。
* `log-line--debug` 的可达性是按代码取值域推的，未在真机观测过。

## 0.8.26

> **单连接可视化**：把核心访问日志里的每条连接与拓扑结合起来看。

### 做了什么

新增「最近连接」列表（时间 / 域名或目标 / 入站 → 出站），支持过滤；
**点一条连接，拓扑图上高亮它经过的路径**（其余压暗），因此可以直接看到
「这条连接从哪个入口进来、走到哪个出口」。

### 数据来自哪里（重要）

核心访问日志里每建立一条连接有一行：
```
accepted tcp:194.221.250.50:443 [tun -> node-n1d232c6b8c7a5004]
```
而**真实域名在另一行**（`sniffed domain:`），且 `accepted` 行**不带连接 ID**，
所以域名只能**按时序配对**（实测相隔 1–70 毫秒）。**这是近似** ——
界面上显示的域名可能配错，详情里给出精确的配对间隔供判断。

**配到域名的比例（真实日志实测）**：`node` 82.8% / `direct` 80.1% /
`api` 0.0% / `dns-out` 0.1% / **总体 48.3%**。
总体偏低**不是算法漏** —— `api` 回环与 UDP 直连**根本没有 sniffed 行**。

### 看不到的东西（如实说明）

* **每条连接的字节数**：核心的统计服务只有聚合计数器，没有逐连接流量；
* **连接的持续时间**：日志只记录建立，不记结束。

所以一条连接只能显示「时间 / 来源 / 目标 / 域名 / 入站 / 出站」。
宁可少显示，不编。

### 一处必须说清的验证边界

本轮**所有**浏览器自动化证据（探针、动画、高亮、压测）都来自
`?preview=1` 的预览页，**不是真实应用**：Tauri 2 在 macOS 用 WKWebView，没有 CDP，
而唯一能看真机 UI 的 WebKit Remote Inspector 需要手工操作、不可自动化。
**「CDP 验证通过」不等于「真机验证通过」。**

## 0.8.25

> 修两处视觉假象：**0 流量的车道不该有车**、**车不该倒着开**。

### 1. 空车道画了 3 辆车

`trucksOnLane` 在 `bytes <= 0` 时返回 3，注释写的是「空车道也画几辆，
否则『量小』与『不通』看不出来」—— 但它把两者画成**一样**（都 3 辆），
既没达成目的，又制造了「明明 0 B 却有车在跑」的假象。

改成返回 0。「量小」与「不通」本来就分得开：卡片字节数分别显示
`↓1.2 MiB` 与 `↓0 B`，不可用时显示「流量不可用」。

（顺带确认：示例里 `http` 入站的 0 是**真的 0** —— 系统代理指向别的软件，
TUN 模式下流量走 `tun`。它不是 dns-out/api 那种测量盲区。）

### 2. 车走回程 → 看起来「倒着开」

路线是闭环（否则车到终点瞬移回起点），但闭环的回程在屏幕上就是倒着开。

中途试过的错解：「走完整圈 + 把回程隐藏」—— 实测隐藏占比 **92.8%**：
整圈是「主干 + 6×来回 + 回主干」，去程只占约 11%，车大部分时间都在
看不见的回程上跑。

正解：进度对**去程长度**取模，车只在去程循环。从最后一个出口回到入口的
那一跳，语义上就是「这趟货送到了」。

### 一处如实说明

回程这一项**没有自动回归测试**。三种几何判据都无法区分「只走去程」与
「走整圈」—— 闭环路径是「去程 + 回程」的重复参数化，同一个屏幕点对应两个
弧长。已在测试文件用 `it.todo` 与 `docs/ui/topology/README.md` 写明局限。

## 0.8.24

> `dns-out` / `api` 不再显示成 `0 B` —— 那是**测量盲区**，改用连接数。

### 问题

用户问「dns-out 和 api 的流量是不是错误的，怎么会是 0」。答案是：**是错的，
但不是「没流量」**。实测这两个出口在大量被使用，而字节计数器恒为 0：

| 出口 | 连接数 | 字节计数器 | 原因 |
|---|---|---|---|
| `dns-out`（协议 `dns`） | **4769** 条 UDP | 0 | Xray 不统计 UDP 出站流量 |
| `api`（本机回环） | **5374** 条 TCP | 0 | Xray 不统计本机回环流量 |
| `block`（`blackhole`） | 201 条 | 0 | **真的 0** —— 连接被拒 |

最小配置实验证实了这点：一条 `accepted tcp:198.18.0.2:53 [socks -> dns-out]`
确实走了 dns-out，而计数器仍是 0。

麻烦在于 `block` 的 0 是**真的 0** —— 于是「真 0」与「测不到」在界面上长得
一模一样。这正是本项目反复修掉的那类混淆（「查不到 ≠ 0」），这次出现在出口侧。

### 改动

1. **dns-out / api 单独分组「内部通道」**，不再进流向图 —— 混在 `node` /
   `direct` 旁边显示 `0 B`，会被读成「这两个出口没在用」；
2. 它们改用**连接数**表示活跃度（唯一可得的指标）。连接数从核心访问日志的
   `accepted ... [in -> out]` 行累计，在核心输出的**单点**解析，不另起 tail；
3. **合计只算用户流向**，不把内部通道的占位 0 混进来；
4. `connections: null` = **没观察到**（核心没跑 / 日志里还没连接行），显示
   「—」而不是 `0`。

## 0.8.23

> 修「地球仪放大后**整屏空白**」；探针补上「看不见故障」的盲区。

### 修复：放大后整屏空白

上一版把 `fitZoom` 改成「近处放大」后，广州↔香港的默认缩放从 1 变成 34.5，
结果**标记与航线全部跑到画布外**、整屏只剩一片纯色。两个潜藏 bug 被缩放同时放大：

| # | 问题 | 为什么 zoom=1 时看不出来 |
|---|---|---|
| 1 | 纬度居中乘了系数（`focus.lat * DEG * 0.6`） | 只偏 48px；34.5× 时偏移被同比放大到 **1287px** |
| 2 | 自转恒为开启 | 整球时只是慢慢转；34.5× 时自转 13.5° 让两点偏出 **2500px** |

正确的居中关系是「经度取负、**纬度直接用 `+φ` 不乘系数**」；
自转改为按「两点跨过的角度」判断，视野张开很小时关闭（否则一转就把视野扫走）。

修复后实测：两点居中对称、相距约 212px，标记与航线都正常显示。

### 探针：补上「看不见故障」的盲区

自检探针原先只量「车 → 引导路径」（恒 ≈0），**车的路径那类故障它根本发现不了**
（实测：故障版本上探针报 0.38px，而真实离线占比 31%）。现改为量
引导路径到**所有可见线**的最短距离，并给出 `off_line_frac` / `off_line_max`。

判据也细化为 `ok / suspect / jump / off_line / unknown`，并明确
**`suspect` 不算失败**：掉帧时动画的补帧策略会让比值自然升到约 2.2，
那是设计而非缺陷。

### 测试

前端 30 条（新增 3 条地球仪视角回归，且**已回退验证**：把两个 bug 改回去，
对应测试会红）。

## 0.8.22

> 这一版的重点是**把「数据乱跳」真正修完**：先有能复现的测试，再逐条修掉，
> 最后独立复核。另外顺手修掉用户最早反馈的「车的路径不对」。
> 测试从 11 条加到 **29 条**。

### 修复：车有约 1/4 的行程「飞在空白处」

这是用户最早说的「车的路径不对」。`routeToD` 把各段曲线用 `C` **连续拼接**，
而 SVG 的 `C` 是从**上一段终点**继续画的 —— 于是「去第 2 个出口」的曲线是从
**第 1 个出口**出发，不回分叉点。隐藏的导引路径（车真的沿它走）因此与屏幕上
可见的线不是同一条。

现在每条支线走完都补一段「回分叉点」，导引路径与可见线的并集重合。实测：

| 量 | 修复前 | 修复后 |
|---|---|---|
| 离线行程占比（车离任何可见线 >1.5px） | **26.2%** | **0.0%** |
| 最大偏离 | **27.12px** | **1.00px** |

（这组数字由四方独立测量得出，修复前分别为 26.2% / 26.2% / 46.7% / 23.8%，
结论一致。）车的颜色也不再按「进度比例」猜，而是按**当前所在的段**取色。

### 修复：车辆数量与流量反向

`trucksOnLane` 里写的是 `Math.log10(1 << 40)`。JS 的位移按 **32 位取模**：
`1 << 40 === 1 << 8 === 256`，于是上限 `hi = log10(256) ≈ 2.41` 比下限
`lo = log10(1) = 0` 的对数域还小 —— 映射**整体反向**：9.6 GiB 的入口只有 3 辆，
而 1 字节也能画 8 辆。改用 `1024 ** 4` 后，同一份预览数据从 **9 辆 → 14 辆**。

### 修复：刷新时位置突变（四处，各有独立判据）

这一轮不再靠「看着不跳」，每条都先写成会失败的测试：

* **入口集合变化**（新增/删除出口）：最大单步 **353.69px → 13.56px**，
  孤立尖峰（最大/中位 > 2）**21 → 0**；
* **车辆数量变化**：状态改为按**稳定身份**（`data-truck-key`，如 `tun#3`）保存，
  不再按数组下标 —— 数量一变，按下标存的进度会整体错位；
* **卡片宽度 / 容器宽度变化**：进度按累积路程走，几何变化不再让同一进度换屏幕点；
* **测量持有已卸载的 label**：`rect = 0` 的锚点不再参与计算。

### 修复：查不到流量时伪装成 `0 B`

核心没在跑、或统计查询失败时，后端把字节数填 `0`，界面就画成 `↓0 B ↑0 B` ——
看起来像「流量掉零」，实际是「这次没查到」。现在后端新增
`traffic_ok`（本次流量是否可信）与 `counter_resets`（累计值跨核心重启续接了几次），
恒有 `traffic_ok === (traffic_error === null)`；界面在 `traffic_ok === false` 时
显示「不可用」而不是 0，且合计不把占位 0 算进去。

顺带：累计计数器加了**单调化**，核心重启后接着累加而不是归零。
**边界**：单调化基准是**进程级** static，所以 **App 重启后基准会丢一次、
数字会真回落一次**（只回落一次，之后继续单调）。

### 修复：拓扑扇面太窄、900px 窗口整图塌掉

* `forkX` 把 70% 的横向空隙给了 3 条水平直线，扇面包围盒只有 **145.2 × 277px**
  （宽高比 0.52，看起来像一堆竖线）。系数改 0.35 后为 **314.6 × 277px**（宽高比 1.14）。
* `@media (max-width: 940px)` 里右列的 `grid-column: 3` 落在隐式列上：
  900px 窗口实测网格 **`501 / 0 / 151`**，**15 条线一条都画不出来**，卡片色条还全变成白色。
  现在为 **`168 / 316 / 168`**，15 条线正常。

### 新增：开发期拓扑自检探针

拓扑动画以前只能在浏览器里「看」，出问题只能临时写 CDP 脚本 —— 每个人口径还不一样。
现在 `?preview=1` 下有一条固定命令：

```js
window.__topologyProbe()                 // 每辆车的 {key, route, progress, x, y, off_path, fill}
window.__topologyProbe({reset: true})    // 先清空帧缓冲再取
// ?preview=1&probeFrames=1800 拉长到约 30 秒的窗口
```

它只读 DOM（不碰组件内部状态），用 `MutationObserver` 观察动画自己写 `transform`
的节奏来取帧，按 `data-truck-key` 配对「同一辆车」，输出
`ratio_max_median`（最大/中位步长）与 `verdict` 三档判读（`≤2` 正常 /
`2–3` 可疑 / `>3` 确定是跳变）。生产构建零残留（实测 `dist/assets/*.js` 中
`__topologyProbe` / `installPreviewBridge` 等各 0 次）。

同一版本预览里还多了个不可用态开关 `?preview=1&...&traffic=unavailable`，
让「查不到流量」这条路径第一次能被截图和回归。

### 修复：预览桥接缺 `unregisterListener`

`@tauri-apps/api/event` 的 `_unlisten` 直接调
`window.__TAURI_EVENT_PLUGIN_INTERNALS__.unregisterListener(...)`，而预览桥接只提供了
`unregisterCallback`，于是预览下每次注册/注销事件都抛异常（实测 9 秒内 8 条）。
**这是预览桥接的缺口，不是产品缺陷**（真实运行由 Tauri 注入该全局）；补上后异常为 0。

### 测试

`apps/ui/src/topologyAnimation.test.ts` 从 11 条加到 **29 条**，新增覆盖：
导引路径必须等于可见线的并集（B1 回归，实测在修复回退版上**变红**）、
`traffic_ok === false` 时不渲染伪 0、车辆数量变化时不误判为跳变。

## 0.8.21

> 修两个问题：**货车跳动**，以及 **dns / api 出口的颜色**。

### 修复：车会「跳一下」

原因是**路线不是闭环**：车走到最后一个出口（终点）后瞬间回到入口（起点），
而这两点在屏幕上相距约 400px。实测确认：最大单步位移 **457px**，而中位只有
**37px** —— 那个 12 倍的孤立尖峰就是这次瞬移。

现在路线是**闭环**：走到最后一个出口后按相反顺序返回各分支 → 回分叉 →
回入口。车顺着走一圈，不再闪现。

顺带修掉两个会让位置突变的隐患：

* 进度改为按**累积路程**推进，而不是「绝对时间 × 比例」——
  拓扑每 2 秒刷新会让路径长度变化，按比例算时长度一变位置就跳；
* 动画的依赖里去掉了位置数组（它们每次刷新都会重建），
  否则动画每 2 秒重启一次、车跳回起点。

修复后实测：最大/中位 = **1.36×**（等速连续），而修复前是 12.4×。

### 修复：dns / api 出口应当是灰色的

颜色表里只有三种去向（经节点 / 直连 / 已拦截），`dns` 与 `api` 取不到值，
fallback 成了蓝色 —— 看起来像「另一条走节点的路」，是误导。

现在它们是**中性灰**，图例也补了「内部通道」一项：这两个是应用自己的
内部通道，不是流向用户的去向。

## 0.8.20

> 拓扑页改成**一个入口 → 全部出口**的一对多扇出。

### 为什么

早先按相邻切段分配出口（一个入口只连自己那几个），看起来像「每个入口有自己
独立的一组出口」—— 不符合事实：**所有入口的流量都经过同一条规则链**，
规则链决定去向，所以任何入口都可能去任何出口。

### 改动

* 每个入口对**全部**出口扇出；
* **每条扇出分支按目的地的出口类别着色**（蓝 = 经节点 / 绿 = 直连 /
  红 = 已拦截），货车走到哪条分支就变成那个颜色 —— 图例因此对得上，
  也能直接看出「这条线通向哪类出口」；
* 线条数变多（入口 × 出口），所以线压暗、主视觉留给货车。

### 一处如实说明

「某个入口的货具体去了哪个出口」核心**没有这个计数器**（只有按入口、
按出口两类统计）。所以一对多表达的是「**任何入口都可能去任何出口**」
这个结构事实，不声称逐条归属。

## 0.8.19

> 地球仪的位置改为**两个数据源交叉校验**，并说明「IP 定位只是估算」；
> 放大上限从 1.8× 提到 **8×**。

### 位置为什么可能不对

IP 地理定位是**尽力而为**，不是确定值。实测三源对照同一个 IP：

| 数据源 | 判定 |
|---|---|
| `ipwho.is` | 云南 · Dali |
| `ipinfo.io` | Dali Old Town |
| `ip-api.com` | 广州市 · 广东 |

而且公网 IP 会变（实测过 `45.207.197.185` / `39.130.21.95` /
`116.53.173.241` 三个），所以「哪一刻查到哪个城市」取决于当时的出口 IP
与用哪个数据源。运营商大内网（CGNAT）与省级骨干出口会让注册地偏离实际城市。

### 改法

* **坐标以 `ipwho.is` 为准**（实测与 `ipinfo.io` 一致）；
* **中文地名用 `ip-api.com`**（支持 `lang=zh-CN`，返回「大理」而不是
  `Dali Baizu Zizhizhou`）；
* 两源坐标相差超过 0.5° 时标为**不一致**，界面显示
  「数据源判定不一致，仅按 IP 归属估算」；
* 界面上把**各源的判定**都列出来，而不是只报一个数字。

### 放大倍数

上限 1.8× → **8×**，可以贴近看城市。代价已在界面上说清：大陆轮廓是
2° 分辨率的位图（约 200 公里一格），放到这个程度海岸线会发虚 ——
它给的是方位感，不是地图精度。

### 顺带的简化

位置查询的 HTTP 从 `xt-core` 移出：那里原本自写 socket，换成 https 需要 TLS。
现在改用项目既有的做法 —— 系统的 `/usr/bin/curl`（零依赖、走系统信任链、
支持 `--interface` 绑网卡）。`xt-core` 只留纯逻辑（解析与合并），
文件 573 → 300 行，测试 4 → 7 条。

## 0.8.18

> 拓扑页删掉了车道列：**线本身就是车道**，货车沿整条路线（入口 → 汇入 → 扇出
> → 出口）行走。布局也因此从四列简化成三列。

### 改动

* 删掉车道列与它的一批样式，以及已被新实现取代的旧覆盖层样式；
* 每条路线是**一条连续路径**，车沿它从头走到尾 —— 连线与货车在同一层，
  不再有「车道里漂的车」和「另一层的线」两套东西。

### 清掉的两个真问题（都不是调参能解决的）

1. **CSS 同名规则后者优先**：定位规则被追加到文件末尾，被后面的同名规则覆盖，
   导致图例占了第 1 列、入口卡片被挤到第 2 列、整幅图错位。
2. **一处 CSS 缺右括号**：它会让后面所有规则被吞进同一条声明，
   表现是「改了样式完全没反应」—— 看起来像调参失败，其实是语法错误。

### 验证

实测：路线长度 479–586px、包围盒横跨整个流动区（入口右缘 → 出口左缘），
货车 9 辆、2.5 秒内 9/9 位置变化。

## 0.8.17

> 拓扑页的**货车改为沿连线行走**：从入口出发、经过车道、沿扇出的弧线抵达出口。
> 之前货车只在自己那条车道里左右漂，和连线毫无关系 —— 看着像两套不相干的东西。

### 改法

一条路线现在是**一条连续路径**（入口弧线 → 沿车道 → 扇出弧线 → 出口），
货车用路径长度采样沿它从头走到尾。连线与货车因此画在同一层，车道那一列
只作为「道路」的视觉底衬。

顺带修掉两个会让观感错乱的问题：

* **路径必须是连续的**：早先每段各写一个 `M`，得到三段互不相连的子路径，
  而路径采样只沿一条连续路径走 —— 货车会在段与段之间跳。
* **动画起始时刻要跨刷新保持**：拓扑每 2 秒刷新会重建动画，
  用局部变量会让货车每 2 秒跳回起点。

### 验证

实测（读 DOM 里的 transform）：路线长度 372–493px、货车 9 辆，
2 秒内 6/6 辆位置都在变化，横向从入口侧 207 走到出口侧 547 ——
确认是沿整条路线行走。

## 0.8.16

> 拓扑页的连线改成**每条车道扇出到出口**（一个源点扇出多条弧线），
> 形状与用户给的草图一致。

### 改动

* 每条车道**直接扇出**到自己那一组出口，不再先汇到一个中间点再分叉；
* 出口按**相邻**切段分配，所以扇形不交叉（试过交错分配，摊得更开但线互相
  穿插，看着乱）；
* 新增一个**扇形专用空列** —— 原来车道列与出口列之间只有 10px，
  曲线横向只跨 4px、纵向跨 129px，看着像一堆竖线而不是扇子。

### 一处如实说明

「哪条车道通向哪个出口」核心**没有这个计数器**（只有按入口、按出口两类
统计）。所以扇形表达的是「车道与出口相连」这个**结构关系**（来自配置），
不声称逐条归属。出口怎么分给车道是**为了画得清楚**，不代表真实流量走向。

### 教训

这一轮改了 8 版才定下来，其中多数问题来自「凭观感调」而不是「先量」：
横向跨度 4px、控制点折回、出口列被挤到 84px —— 每个都是量出来的具体数字，
盯着截图猜不出来。已记进 `docs/ui/topology/`。

## 0.8.15

> 地球仪支持**缩放**（滚轮与按钮），默认视角改成**先看全整个地球**。

### 新增：缩放

* **滚轮**在画布上缩放；**放大 / 缩小 / 复位**三个按钮。
* 范围 0.55–1.8 倍。上限是按「球缘要看得见」定的 —— 再放大球缘就溢出画布，
  只剩一片海岸，反而失去方位感。

### 默认视角：先看全地球，必要时才缩小

打开时对准**航线两端的中点**（`bytes` 是整条航线的量，不属于某一端），
缩放默认保证**整个地球可见**。

只有航线跨得很远、两端装不下时才自动缩小。近距离航线（例如广州↔香港
仅 129 公里）**不再放大** —— 早先的公式会把它放大到接近上限，球占满画布、
只剩一片海岸，反而看不出「从哪飞到哪」。要看细节可以用滚轮自己放大。

### 验证

读画布自身的像素来量球径（截图会被裁剪，量不准）：

    默认 606 → 缩小 ×3 得 334 → 放大 ×1 得 418 → 复位回 606 → 滚轮向下得 542

新增 6 条前端测试覆盖视角计算（缩放范围、对准中点、无流量退化、越远越小、
两点重合不除零）。

## 0.8.14

> 拓扑页加上**连线**：入口汇聚到车道、车道再分流到各个出口，像一座立交桥。
> 同时把车道改为**垂直居中**（0.8.13 的内容一并在这版发布）。

### 新增：入口 → 车道 → 出口 的连线

* 入口卡片用绿线汇聚到车道组；
* 各车道用灰线汇到右侧一点；
* 汇合点用蓝线分流到各个出口卡片。

位置按**实测的元素矩形**绘制，并用 `ResizeObserver` 跟随布局变化 ——
流量数字每两秒更新一次、宽度会变，量一次就画会错位。

### 连的是「结构关系」，不是「归属」

四条车道是同一个规则链前面的队列，不是四个独立通道；路由规则决定流量从
哪个入口去哪个出口。所以连线表达「汇入同一条主干、再分流」这个**配置事实**，
而不是假装「入口 i 对应车道 i」。这与车道颜色用的是同一套口径（核心没有
逐条规则的计数器）。

### 同时包含 0.8.13：车道垂直居中

原来车道被顶在上边（实测车道高 66px、出口列高 415px），看着像挂在左上角。
现在居中，左右两列与车道中心对齐（实测偏差小于 3px）。

## 0.8.13

> 拓扑页的车道改为**垂直居中**，左右两列与它对齐成一条完整的公路。
> （该版本号只提升了版本与说明，功能改动合并进 0.8.14 发布。）

## 0.8.12
